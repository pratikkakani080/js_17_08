// Arithmatic operators
    // + (Addition)
    // - (Subtaction)
    // * (Multiplication)
    // ** (Exponentiation)
        // console.log(4**3);
    // / (Division)
    // % (Modulo)
        // console.log(5 % 2); (Reminder)
    // ++ (increment)
    let a = 4
    // a++
    // a++
    // a++
    // -- (decrement)
    // a--
    // a--
    // a--
    // a--
    // a--

    // Assignment Operators
    // a += 5
    // -+, /=, %=, *=, **=
        // console.log(a);
        
    // Comparision operator
    // == (equality)
    // === (strict equality)
    // console.log(a !== '4');
    // !=, !==, <, >, <=, >=

    // && 
    let color = 'red'
    let shape = 'round'
    let size = 'big'
    let statement = 'color is red and shape is round'
    // console.log(color === 'red' && shape === 'round' && size === 'big'); // if all conditions are true -> true else false
    
    // ||
    // console.log(color === 'red' || shape === 'round' || size === 'big'); // if any one condition is true -> true else false
    
    
    // exclamation mark ( logical not operator )
    // console.log(!'   vvdfsfds  ');
    
    
    // ternary operator
    // condition ? true statement : false statement
    console.log(color === 'red' && shape === 'round' ? statement : 'color not defined');

    // chaining operator
    const obj = {
        data: 'test'
    }
    console.log(obj.test?.test);
    
    // ?.
    
    // spread operator
    // ...
        
        